diff --git a/configure b/configure
index 0286fb1..01665fd 100755
--- a/configure
+++ b/configure
@@ -381,6 +381,11 @@ include = "/home/rustbuild/root64/include"
 rustc-flags = "-l ssl:static -l crypto:static -l dl -L /home/rustbuild/root32/lib"
 root = "/home/rustbuild/root32"
 include = "/home/rustbuild/root32/include"
+
+[target.arm-unknown-linux-gnueabihf.openssl]
+rustc-flags = "-l ssl:static -l crypto:static -l dl -L /usr/lib/arm-linux-gnueabihf"
+root = "/usr"
+include = "/usr/include"
 EOF
     fi
 fi
